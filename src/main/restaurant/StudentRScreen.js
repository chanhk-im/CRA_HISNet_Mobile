import React from 'react';
import { View, Text } from 'react-native';

export function StudentRScreen() {
    return (
        <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
            <Text>학생식당</Text>
        </View>
    );
}