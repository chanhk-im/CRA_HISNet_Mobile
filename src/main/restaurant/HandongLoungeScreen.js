import React from 'react';
import { View, Text } from 'react-native';

export function HandongLoungeScreen() {
    return (
        <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
            <Text>한동라운지</Text>
        </View>
    );
}